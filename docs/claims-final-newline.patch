diff --git a/grammars/claims.gbnf b/grammars/claims.gbnf
--- a/grammars/claims.gbnf
+++ b/grammars/claims.gbnf
@@ -1,6 +1,6 @@
 # Section 4.1: one or more claims, each on its own JSON line.
-root ::= claim+
-claim ::= "{" ws "\"text\":" string "," ws "\"src\":" string "," ws "\"rel\":" rel "," ws "\"dst\":" string ws "}" nl
+root ::= claim (nl claim)* nl?
+claim ::= "{" ws "\"text\":" string "," ws "\"src\":" string "," ws "\"rel\":" rel "," ws "\"dst\":" string ws "}"
 rel ::= "\"is_a\"" | "\"defends_against\"" | "\"enables\"" | "\"requires\"" | "\"contradicts\"" | "\"example_of\"" | "\"part_of\""
 # TODO(CLAIMS-GRAMMAR): section 4.1 omits these referenced JSON lexical rules.
 string ::= "\"" ([^"\\\x00-\x1F] | "\\" (["\\/bfnrt] | "u" [0-9a-fA-F]{4}))* "\""
